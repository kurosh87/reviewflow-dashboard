# ReviewFlow Dashboard Configuration
# Copy to .env.local and fill in your values

# WordPress Site URL (where ReviewFlow plugin is installed)
WORDPRESS_URL=https://your-wordpress-site.com

# WordPress Application Password
# Create at: WordPress Admin → Users → Profile → Application Passwords
WORDPRESS_USERNAME=admin
WORDPRESS_APP_PASSWORD=your-app-password-here

# Supabase (Optional - for direct database access)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# NextAuth Secret (for authentication)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-random-secret-here

# Environment
NODE_ENV=development
